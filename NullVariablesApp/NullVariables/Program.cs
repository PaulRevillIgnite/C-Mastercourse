

// null is the lack of a value

// ? allows the variable to be nullable
int? age = null;
bool? birthday = null;
double? height = null;
decimal? weight = null;

// Strings are nullable by default so don't need the ?, but it's reccomended to add it for clarity
string firstName = null;
string? LastName = null;